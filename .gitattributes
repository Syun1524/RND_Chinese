# 行尾与二进制属性
#
# 影片字幕 *.ass 是「随补丁分发」的资产：它由 subs/tools/build_lyric_subs.py
# 生成（保留原始 CRLF），最终交给 Windows 的 VSFilter 读取。
# 若交给 core.autocrlf 处理，提交时 CRLF 会被改写成 LF——仓库里的字节就不再等于
# 实机验证过的字节（本项目要求字幕逐字节可复核）。故显式关闭转换。
*.ass -text

# 译稿与日文基线 *.msb.txt 同样是逐字节可复核的内容（要喂给 sc3tools 回写）。
# 仓库里现为 CRLF，而本机 core.autocrlf=true —— 任何一次裸 git add 都会把它们
# 整批刷成 LF（2026-09-13 整理时误触过一次）。显式 -text 后 git 不再做任何转换，
# 与 *.ass 同理：仓库字节 == 实机验证过的字节。
*.msb.txt -text

# 批处理 *.bat：cmd 按行读取批处理文件，LF 换行会让部分行被静默吞掉
# （本项目踩过：bat 里带 LF 时命令不执行却"成功退出"）。强制工作区为 CRLF。
# 仓库内仍按 text 规范化存 LF（默认行为），检出时统一还原成 CRLF。
*.bat text eol=crlf
