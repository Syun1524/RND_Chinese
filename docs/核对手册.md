# 服装核对指南

> 目的：把 **11 个多变体角色的 61 套服装**逐一认出来（哪套是水着、哪套是体操服、哪套是私服……）。
> 单变体角色（c007/c009/c010/c011/c013/c014/c015/c016 共 8 个）没有别的服装可切，不列入。
>
> CoZ 泳装那 10 套已确认（见下表），剩 **51 套**待认。

## 一、为什么需要核对

游戏把每个角色的每套服装存成一个独立的 `.lkm` 模型（`c002_100.lkm` 这种），
但**文件里不含任何服装名称**。已逐项排除过的可能来源：

| 可能的来源 | 结论 |
|---|---|
| `.lkm` 模型二进制 | 只有网格 + 内嵌贴图，无名称串 |
| `.scx` 脚本 | 压缩格式，全文无 `c0NN_MMM` 形式引用，不引用变体名 |
| `.lka` 动作文件 | 只有动画名（`stand`/`walk_loop`），无服装名 |
| 游戏文本（msb/enscript） | 只有通用词汇（水着/体操服/ブルマ/メイド/猫耳/浴衣） |
| 系统菜单文本 | **游戏根本没有换装菜单**（这正是 CoZ 要用 fileIdRemap 硬改的原因） |
| CoZ 原版补丁 | `swimsuitPatch` 只点名了 10 个泳装模型，无描述 |
| 邻接补丁 glossary | 只有泛化术语，无按模型映射 |
| `.lkm` 内嵌贴图 | **可解出来看** —— 这是唯一可行的离线线索 |

所以：**离线看贴图认，剩下拿不准的进游戏实地看。**

## 二、已确认的 10 套（CoZ 泳装补丁，100% 可信）

CoZ 的 `c0data.cls` 里点名了这些文件，其 `swimsuitPatch` 指向它们：

| 角色 | 泳装变体 | fileId |
|---|---|---|
| c001 | 070 | 469 |
| c002 | 100 | 474 |
| c003 | 090 | 485 |
| c004 | 120 | 496 |
| c005 | 080 | 502 |
| c006 | 090 | 509 |
| c008 | 030 | 512 |
| c012 | 020 | 518 |
| c017 | 040 | 524 |
| c019 | 020 | 531 |

## 三、路线 A：离线看图（先做，能省大半时间）

```
成品ing/服装核对/
  服装对比_<角色>.png       ★ 主用
  服装对比_index.html       可点选浏览页（含上面全部大图）
  服装名称表.json           已预填 CoZ 泳装那 10 套，其余留空待填
  sheets/variant/*.png      单个变体的全部贴图（含脸/眼睛等通用件）
  sheets/grid/<角色>.png    横排对照（行=变体，列=贴图序号；序号会错位，仅参考）
  服装贴图统计.json         每个变体有几张独有贴图
```

**只看 `服装对比_<角色>.png` 就够了。** 每行一个变体，只显示**该变体独有的贴图**
（脸/眼睛/嘴/身体等所有变体共用的已按内容哈希剔除），通常只剩 3~10 张 —— 那就是服装本体。
图里有一列「服装名（填这里）」，直接对照着填。

> 贴图是 UV 展开图（不是成衣效果图），所以看的是**配色 / 花纹 / 部件**。
> 拿不准的（例如 c001_070 的纹样看着像和服但 CoZ 标注它是泳装）**进游戏确认，别硬猜**。

## 四、路线 B：进游戏确认（拿不准的用这个）

工具是独立 exe：`RNDZhOutfitTool.exe`（**不随补丁发布**，只给开发者用）。
已放在两个游戏根目录各一份：

- `D:\Ruanjian\Steam\steamapps\common\ROBOTICS;NOTES DaSH\`
- `D:\ZZGAME\ROBOTICS NOTES DaSH\`

### 批量核对（推荐）

面板给**每个角色一行**（11 行），各选一套。可以**一次固定多个角色，一次进游戏同时看**：

1. 运行游戏根目录的 `RNDZhOutfitTool.exe`
2. 先用「**全体：各角色第 k 套**」一键铺开——它把每个角色都设成自己的**第 k 个变体**
   （注意是"第 k 个"，不是"变体号 k"：各角色编号不一致，c001 从 010 起、c005 从 030 起，
   行内下拉会显示实际选中的编号）
3. 再逐行微调；某行选「**不动**」表示该角色这次不参与
4. 点「开始游戏」→ 进游戏**一次看完这些角色**穿的是什么
5. 退出 → 改选择 → 再进

**状态行**会实时显示已固定哪些角色、各自是哪套。所以整轮核对只需十几次启动，
而不是每套一次。

### 单套精查

某个角色认不出来时，把其它行都设成「不动」，只留那一行，逐套切换（下拉里直接选）反复进游戏看。

### 注意

- **不需要**先在启动器里开「泳装／换装模式」（见下方原理）。
- 核对是「把该角色的**全部**变体都指向选中的那一套」，所以你看到的是"这个角色穿了这一套"。
  角色在哪出现取决于剧情 —— 找个该角色出镜的场景看。

### 原理（备查）

- 每个角色一个 **bool 开关**，写在 `%LOCALAPPDATA%\Committee of Zero\RNDSteam\config.json`，
  形如 `"zz_c002_100": true`。**别手改**，工具会写（每次点击即刻落盘）。
  选中的键才写、没选的留空，所以任意多个角色能同时生效。
- 对应 `patchdef.json → settings.zz_<角色>_<变体>`，
  内含该角色**全部变体（含目标自身）→ 目标变体**的 `fileIdRemap`。
- 两个坑（都会**静默失效**）：
  1. 目标必须也映射到自己。`swimsuitPatch` 会把该角色每个变体（含目标本身）都指向泳装，
     而 `json_merge` 是**递归覆盖、删不掉键** —— 漏掉自身映射的那套会被泳装覆盖回去。
  2. 键名必须以 `zz_` 开头（保证字典序排在 `swimsuitPatch` 之后 → 核对胜出）。
     改成排在前面的名字会先合并、再被泳装覆盖。
- **核对不依赖「泳装／换装模式」。** `mgsFileOpenHook` 读 `config["patch"]["fileIdRemap"]`
  是**无条件**的，且命中后直接 `return`（不会落到 `fileRedirection`）。所以：
  - 泳装模式**开** → 被核对的角色穿选中的那套，其它角色穿泳装
  - 泳装模式**关** → 被核对的角色穿选中的那套，其它角色穿原版默认服

  两种都能核对，关着时画面干扰更少（推荐）。
- 只影响被核对到的角色；没选的角色不受影响。
- 两个程序的配置互不干扰：启动器保存时会**原样保留未知键**，
  所以它不会把工具写的 `zz_*` 抹掉。

## 五、认出来之后

把名字填进 `成品ing/服装核对/服装名称表.json`：

```json
{
  "c001_010": "制服",
  "c001_070": "水着",
  "c002_100": "水着"
}
```

然后（可选，让启动器/工具显示中文名）：

```bash
python scripts/add_outfit_choices.py --names 成品ing/服装核对/服装名称表.json
python scripts/gen_outfit_table.py
cd launcher && build_launcher.bat && build_outfit_tool.bat
```

## 六、重新生成素材

```bash
python scripts/build_outfit_diff_sheets.py   # 服装对比图（主用，带填名栏）
python scripts/build_outfit_sheets.py        # 单套图 + 横排对照 + 分变体 index.html
python scripts/gen_outfit_table.py           # launcher/outfit_table.h
python scripts/add_outfit_choices.py         # patchdef 的 61 个核对项
```

数据源：`成品ing/服装映射表.json`（角色 → 变体 → fileId，由解包日志推导）。
模型本体在 `解包/解包cpk的产物(日语)/model/`。

## 七、已知事实（不要再重复查证）

- **19 个角色 / 69 套变体**；多变体角色 11 个，共 61 套需核对，其中 10 套已确认。
- 69 套**两两内容都不同**（已做全量 md5 对比），没有可合并的。
- `.lkm` **内嵌贴图**：`<uint32 大小>"DDS "` 记录，2048×2048 DXT1/DXT5，可直接解出。
- 贴图**槽位顺序在不同变体间会错位**（c004_070 把脸部贴图排在最前，c004_010 排最后），
  所以一律按**内容哈希**匹配，不能按索引。
- 每个变体的服装贴图通常只有 **3~10 张**，其余十几张是全变体共用的脸/眼睛/身体。
- motion 文件名泄漏角色名：`c003_070_frau@rnd_dance_5.lka` →
  c002=akiho、c003=frau、c004=junna、c007=kou、c013=kou、c018=daru。
  这些是**变体号 + 角色名**，但给不出服装名。

## 八、套装类型 × 角色矩阵

> 按「服装类型」看谁有哪套。括号内是该类型的细分（如猫耳的粉/蓝）。
> 「—」= 该角色没有这个类型（或尚未认出）。各角色第 1 套 = 默认装，不列入。

| 角色 | 和服 | 泳装 | 体操服 | 猫耳 | 校服 |
|---|---|---|---|---|---|
| **海翔** c001 | 060 | 070 | — | — | — |
| **秋穗** c002 | 090 | 100 | 110 | 070(粉)、120(蓝) | — |
| **芙劳** c003 | 080 | 090 | 100 | 110 | — |
| **淳和** c004 | 070 | 080 | 100 | 130 | — |
| **爱理** c005 | 050 | 080 | 060 | 090 | 070 |
| **昴** c006 | 080 | 090 | — | — | — |
| **绹** c017 | 050 | 040 | 060 | 070 | — |
| **c008** c008 | — | 030 | — | — | — |
| **c012** c012 | — | 020 | — | — | — |
| **c019** c019 | — | 020 | — | — | — |


### 从矩阵反推未认服装的位置

- **海翔**（4 套：默认/050/和服/泳装）：缺体操服、猫耳，只剩 050 一个空位 → 050 是二者之一。
- **昴**（6 套）：缺体操服、猫耳，剩 030/050 两个空位 → 大概率各占一个。
- **秋穗**（10 套）：四类已齐 + 双猫耳，剩 030/130/140/150 都是**额外**服装（私服、女仆之类）。
- **芙劳**（8 套）：四类已齐，剩 050/060/070 为额外服装。
- **淳和**（10 套）：四类已齐，剩 050/060/090/110 为额外服装。
- **爱理**（6 套）：默认+四类+校服，**已全部认完**。
- **绹**（5 套）：默认+四类，**已全部认完**。
- c008（3 套）：有泳装(030)，010/040 未认。c012（2 套）：有泳装(020)，010 未认。
- c019（5 套）：有泳装(020)，010/030/040/050 未认（角色身份未确认）。
