# 打包清单（自研安装包用）

> 目的：把"补丁包体"和"安装器程序"分开。**本文件只管包体（数据/文档）**，安装器程序另行开发。
> 前提：不使用 CoZ 官方安装器（`nguninstall.exe` / `ngreceipt.bin`），也不参考隔壁方案。
> 基准：工作区 `RND补丁英文成品原版/` 是 CoZ 英文补丁的成品包，可作**文件结构**参照（仅结构，不含内容）。

---

## 0. 一句话

包体 = **原版游戏文件之外的全部补丁文件**。判据：原版 Steam 内容里有 `Game.exe`、各 `.cpk`、`launcher.exe`、`facial.bin` 等；这些**不进包**，其余进包。

---

## 1. 目录结构（相对游戏根目录）

```
<游戏根>/
├── LauncherC0.exe            ← 启动器（未来替换为自研）
├── libcurl.dll               ← 启动器/更新用
├── Qt5Core/Gui/Svg/Widgets.dll  ← 启动器（Qt5）依赖
├── VSFilter.dll              ← 影片字幕渲染（DirectShow）
├── boot.bat                  ← start launcher.exe JP
├── steam_appid.txt
├── proton_boot_fix.sh        ← Linux/Proton
├── RNDPatch-README.txt       ← 补丁说明（换成中文版，见 §4）
└── NOTES DaSH/               ← ★关键：dinput8.dll 在这一层
    ├── dinput8.dll           ← ★LanguageBarrier 运行时（补丁核心）
    ├── VSFilter.dll
    ├── d3d9 / d3d10 / d3d10_1 / d3d10core / d3d11 / dxgi
    └── languagebarrier/
        ├── patchdef.json     ← 补丁定义（码表/字体/重定向）
        ├── gamedef.json      ← 游戏定义（SigScan 特征码）
        ├── defaultconfig.json← 默认用户配置（见 §4）
        ├── versioninfo.json  ← 更新检查（见 §4）
        ├── stringReplacementTable.bin
        ├── c0data.cls + c0data/   ← UI/背景图集 + 泳装模型
        ├── enscript.cls + enscript/ ← 译文脚本（.msb）
        ├── fonts/            ← ★只放 .otf
        ├── subs/             ← 影片字幕 .ass + subs/fonts/
        ├── LICENSE / THIRDPARTY.LB.txt / THIRDPARTY.patch.txt  ← 合规，必须带
        └── defaultconfig.json
```

---

## 2. 必须包含

| 位置 | 内容 | 现状大小 |
|---|---|---|
| `NOTES DaSH/dinput8.dll` | 补丁 DLL（含数字 9 修复 + 字体兜底） | 3.2 MB |
| `languagebarrier/enscript/` | **323 个 `.msb`** 译文 | 2.7 MB |
| `languagebarrier/c0data/` | **83 个 `.png`**（UI/背景图集） | 129.8 MB |
| `languagebarrier/c0data/` | **10 个 `.lkm`**（泳装/换装模型，见 §5） | 247.8 MB |
| `languagebarrier/subs/` | 15 个 `.ass` + `subs/fonts/`（10 个字体） | 52.4 MB |
| `languagebarrier/fonts/` | **仅 `NotoSansCJKsc-Regular.otf`** | 16.4 MB |
| `languagebarrier/` | `patchdef/gamedef/defaultconfig/versioninfo.json`、`*.cls`、`stringReplacementTable.bin` | <1 MB |
| `languagebarrier/` | `LICENSE`、`THIRDPARTY.LB.txt`、`THIRDPARTY.patch.txt` | ★合规必需 |
| 根目录 | 启动器及其依赖、`VSFilter.dll`、`boot.bat` 等 | 见 §1 |

> 包体合计约 **450 MB**（不含字体缓存）。其中泳装模型占 248 MB，是最大单项。

---

## 3. 必须排除（含理由）

| 排除项 | 理由 |
|---|---|
| `fonts/font_*.dds`、`outline_*.dds`、`fontData.bin` | **运行时字体缓存**（17 字号 × 2 套 ≈ 414 MB）。见 §5「字体缓存」 |
| `*.bak*`、`*_backup*`、`备份/` | 备份文件，不属于发布内容 |
| `languagebarrier/enscript/txt/` | sc3tools 提取中间产物 |
| `languagebarrier/enscript/sc3tools.exe` | 误留在 enscript/ 的开发工具（应挪走或删除） |
| `languagebarrier/analyze_dds.py` | 开发脚本 |
| `fonts/simhei.ttf` | **Windows 系统字体，不可分发** |
| `fonts/noto.ttc` | 日文字体，中文版不需要 |
| `Game.exe`、`*.cpk`、`launcher.exe`、`facial.bin` 等 | 原版游戏文件 |

---

## 4. 配置项（发布前必须定）

### 4.1 `versioninfo.json`
当前 `updateCheckUrl` 已从 CoZ 改为本仓库：
```
https://raw.githubusercontent.com/Syun1524/RND_Chinese/main/update.json
```
**未发布 `update.json` 时启动器只会提示"检查失败"**（无害），不会再指向 CoZ 英文补丁。
发布后往仓库 `main` 放 `update.json` 即可启用更新提示。

### 4.2 `defaultconfig.json`（用户默认配置）
`rawConfig = merge(defaultconfig, 用户配置)`，所以**改这里 = 改所有玩家的初始状态**。
- 要**默认开启泳装模式** → 加 `"swimsuitPatch": true`
- `fontPath` 建议与 `patchdef.base.fontPath` 一致（现在都指向 `NotoSansCJKsc-Regular.otf`）

### 4.3 `appdatadir`（⚠ 现有实机 bug，发布前必改）
`patchdef.json` 的 `appdatadir` 决定用户配置落在哪个目录：
```
路径 = %LOCALAPPDATA%\<appdatadir>\config.json
```
- CoZ 原值：`Committee of Zero\RNDSteam`（启动器 `LauncherC0.exe` 正是写这里）
- **当前 CN 实机值是空串 `""`** → 配置落到 `%LOCALAPPDATA%\config.json`，与启动器写的目录**不是同一个**
  → **后果：启动器里所有开关（泳装/鼠标/卡拉OK…）对 CN 补丁无效**

发布前二选一：
- **A**：`appdatadir` 设成 `Committee of Zero\RNDSteam`（与 CoZ 启动器一致，开关立即生效）
- **B**：自研启动器，把 `appdatadir` 设成自己的目录名，并让启动器读写同一路径

### 4.4 `patchName` / `patchVersion`
现为 `ROBOTICS;NOTES DaSH Chinese Patch` / `0.1`，建议改成正式名与版本号。

---

## 5. 字体缓存：不要预置

- `font_*.dds` / `outline_*.dds` / `fontData.bin` 是**运行时按当前码表烘焙**的产物（约 414 MB）。
- 不预置的理由：包体 +414 MB；且缓存带 `charsetHash` 指纹，**玩家侧码表/字体一旦不同就整份作废重烤**，预置收益不稳定。
- 代价：首启要烘焙（多进界面逐步烤全字号），**正是崩溃高发窗口**——已通过字体兜底（`missingGlyph`）降低风险。
- 若日后确定要预置：必须**保证玩家侧码表与字体与烘焙时逐字节一致**（自包含安装包可做到），并接受包体 +414 MB。

---

## 6. 泳装 / 换装模式

- 实现方式：`patchdef.json` 的 `settings.swimsuitPatch.setters.fileRedirection.model`，把 48 个 `model` fileId 重定向到 c0data 的 10 个 `.lkm`。
- 对应文件：`c0data/` 下 `c001_070.lkm` … `c019_020.lkm`（共 10 个，248 MB）——**必须进包**，否则开启后换装模型加载失败。
- 开关：用户配置的 `swimsuitPatch`（见 §4.2 / §4.3）。当前实机已设为 `true` 供测试。

---

## 7. 字体授权

- 只用 **`NotoSansCJKsc-Regular.otf`**（SIL Open Font License，允许再分发）。
- 分包时把字体自身的 OFL 许可一并附上。
- 保留 `languagebarrier/LICENSE`、`THIRDPARTY.LB.txt`、`THIRDPARTY.patch.txt`（CoZ LanguageBarrier 许可）。

---

## 8. 安装器需实现（程序部分，未来自研）

1. 定位游戏目录（注册表 / Steam `libraryfolders.vdf` / 手动选择；确认同级有 `script.cpk`/`system.cpk`）
2. 版本校验（`Game.exe`、`script.cpk`、`system.cpk` 的哈希比对；不匹配则停止）
3. 覆盖前**备份**原文件
4. 写入包体文件
5. **清除旧字体缓存**（`font_*.dds`/`outline_*.dds`/`fontData.bin`）——码表变更后旧缓存会错位
6. 写入自研的卸载信息（**不要沿用 CoZ 的 `nguninstall.exe` 注册项**）
7. 可选：写入 `appdatadir` 对应目录的默认 `config.json`

---

## 9. 发布前检查

- [ ] §3 的排除项全部不在包里（尤其字体缓存、`*.bak`）
- [ ] `NOTES DaSH/dinput8.dll` 是**最新**构建（含数字 9 修复 + 字体兜底）
- [ ] §4.1 update 地址、§4.2 默认配置、§4.3 `appdatadir`、§4.4 名称版本
- [ ] §6 泳装 `.lkm` 已进包，`swimsuitPatch` 默认值已定
- [ ] 许可文件齐全
- [ ] 干净机器实测：装 → 玩 → 卸 → 游戏回原版
